import { StyleSheet, Text, View } from "react-native";
import { AppHeader, Card, Screen } from "@/components/pieceful-ui";
import { mobileThemes } from "@/constants/pieceful-theme";
import { useApp } from "@/state/app-provider";

const shortcuts: [string, string, string][] = [["R", "Girar peça", "Rotate piece"], ["H", "Usar dica", "Use hint"], ["Espaço", "Mover tabuleiro", "Move board"], ["+ / −", "Controlar zoom", "Control zoom"], ["Esc", "Fechar painel", "Close panel"]];
export default function KeyboardHelpScreen() { const { t, theme } = useApp(); const colors = mobileThemes[theme]; return <Screen><AppHeader title={t("Teclado", "Keyboard")} showTitle back /><Text style={[styles.intro, { color: colors.muted }]}>{t("Atalhos para teclado externo no tablet ou para a versão desktop.", "Shortcuts for an external tablet keyboard or the desktop version.")}</Text><Card style={{ padding: 0, overflow: "hidden" }}>{shortcuts.map(([key, pt, en], index) => <View key={key} style={[styles.row, { borderBottomColor: `${colors.muted}28`, borderBottomWidth: index === shortcuts.length - 1 ? 0 : StyleSheet.hairlineWidth }]}><View style={[styles.key, { backgroundColor: colors.panelAlt, borderColor: `${colors.accent}55` }]}><Text style={[styles.keyText, { color: colors.accent }]}>{t(key === "Espaço" ? "Espaço" : key, key === "Espaço" ? "Space" : key)}</Text></View><Text style={[styles.action, { color: colors.text }]}>{t(pt, en)}</Text></View>)}</Card></Screen>; }
const styles = StyleSheet.create({ intro: { fontFamily: "Inter_400Regular", fontSize: 14, lineHeight: 20, marginBottom: 16 }, row: { minHeight: 72, flexDirection: "row", alignItems: "center", gap: 16, paddingHorizontal: 16 }, key: { minWidth: 72, height: 40, borderRadius: 11, borderWidth: 1, alignItems: "center", justifyContent: "center", paddingHorizontal: 10 }, keyText: { fontFamily: "Inter_700Bold", fontSize: 12 }, action: { flex: 1, fontFamily: "Inter_600SemiBold", fontSize: 15 } });
